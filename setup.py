from setuptools import setup, find_packages

setup(name="util", packages=find_packages())
setup(name="bfv", packages=find_packages())
setup(name="ckks", packages=find_packages())
setup(name="tests", packages=find_packages())